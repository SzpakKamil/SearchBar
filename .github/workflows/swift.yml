name: Swift

on:
  push:
    branches: [ "1.0", "2.0", "2.1" ]
  pull_request:
    branches: [ "1.0", "2.0", "2.1" ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-macos-14-5-9:
    name: Build macOS 14 (Swift 5.9)
    runs-on: macos-14

    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode 15.0
      run: |
        sudo xcode-select -s /Applications/Xcode_15.0.app/Contents/Developer
        swift --version

    - name: Build
      run: swift build -v

    - name: Run tests
      run: swift test -v

  build-macos-14:
    name: Build macOS 14 (Swift 5.10)
    runs-on: macos-14

    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode 15.4
      run: |
        sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
        swift --version

    - name: Build
      run: swift build -v

    - name: Run tests
      run: swift test -v

  build-macos-15:
    name: Build macOS 15 (Swift 6.0)
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode 16.0
      run: |
        sudo xcode-select -s /Applications/Xcode_16.0.app/Contents/Developer
        swift --version

    - name: Build
      run: swift build -v

    - name: Run tests
      run: swift test -v

  build-macos-26:
    name: Build macOS 26 (Swift 6.2)
    runs-on: macos-26

    steps:
    - uses: actions/checkout@v4

    - name: Select Xcode 16.2
      run: |
        sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer || sudo xcode-select -s /Applications/Xcode_26.2.app/Contents/Developer
        swift --version

    - name: Build
      run: swift build -v

    - name: Run tests
      run: swift test -v

  build-ios:
    name: Build iOS (Latest Swift)
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Build for iOS
      run: |
         xcodebuild -scheme SearchBar -destination 'platform=iOS Simulator,name=iPhone 16 Pro,arch=arm64' build test

  build-tvos:
    name: Build tvOS (Latest Swift)
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Build for tvOS
      run: |
        xcodebuild -scheme SearchBar -destination 'platform=tvOS Simulator,name=Apple TV,arch=arm64' build test

  build-watchos:
    name: Build watchOS (Latest Swift)
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Build for watchOS
      run: |
        xcodebuild -scheme SearchBar -destination 'platform=watchOS Simulator,name=Apple Watch Series 11 (46mm),OS=26.2,arch=arm64' build test

  build-visionos:
    name: Build visionOS (Latest Swift)
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v4

    - name: Build for visionOS
      run: |
        xcodebuild -scheme SearchBar -destination 'platform=visionOS Simulator,name=Apple Vision Pro,OS=26.2,arch=arm64' build test
